{
  "name": "pairtreefamily",
  "version": "1.0.0",
  "description": "root project",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -b -w",
    "ms-dev": "npm run dev -w @pairtreefamily/my-app"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
